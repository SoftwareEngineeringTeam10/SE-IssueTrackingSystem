plugins {
    // toolchain JDK 21이 없는 환경에서 자동 다운로드되도록 resolver 등록
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'SE-IssueTrackingSystem'

include 'ui-javafx'
include 'ui-swing'
include 'core'